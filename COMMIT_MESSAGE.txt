Fix OAuth authentication UI and backend ADC creation

## Issues Fixed

1. **OAuth button missing in single-project mode**
   - OAuth button now appears for both single-project and cross-project setups
   - Added conditional messaging and button text based on project configuration

2. **Duplicate OAuth buttons in cross-project mode**
   - Removed duplicate OAuth button from Gemini CLI Project ID section
   - Kept single OAuth button in authentication method section

3. **Backend using incorrect gcloud command**
   - Changed from `gcloud auth login` to `gcloud auth application-default login`
   - Fixed command flags: removed `--brief`, added `--quiet`
   - Updated return value to indicate ADC creation

## Files Modified

Frontend:
- app/frontend/src/components/ConfigForm.tsx
  * Updated OAuth button condition (line 399): show for all OAuth auth
  * Removed duplicate button (deleted lines 262-282)
  * Added conditional messaging and button text (lines 402-418)

Backend:
- app/backend/services/auth_service.py
  * Updated authenticate_oauth_flow() to create ADC (lines 243-327)
  * Changed command to use application-default login
  * Updated function documentation
  * Modified return value to include adc_created flag

Documentation:
- app/CHANGELOG.md (new file)
  * Detailed changelog of authentication fixes
- app/README.md (previously updated)
  * Already contains dual authentication method documentation

## Testing

- Backend endpoint tested: `/api/authenticate-gemini-oauth` ✅
- ADC creation verified with correct gcloud command ✅
- Frontend UI verified: OAuth button appears in single-project mode ✅
- Frontend UI verified: No duplicate buttons in cross-project mode ✅
- Build status: Both frontend and backend compiling successfully ✅

## Impact

Users can now successfully authenticate in all four scenarios:
- Single-project + OAuth ✅
- Single-project + Vertex AI ✅
- Cross-project + OAuth ✅
- Cross-project + Vertex AI ✅
